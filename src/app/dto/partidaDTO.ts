export interface PartidaDTO {
  idPartida:number;
  codPartida: string;
  partida: string;
  und: string;
  total: number;
}

export interface CrearpartidaDTO {
  idPartida:number;
  idPadre:number;
  codPartida: string;
  partida: string;
  und: string;
  total: number;
}

